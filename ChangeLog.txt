(in-progress):

  * Pin appimagetool to release 1.9.1 (was the rolling 'continuous' branch).
  * Bundle a pinned, checksum-verified AppImage type2 runtime (20251108) and
    inject it into appimagetool by default, so AppImage builds no longer
    download the rolling 'continuous' runtime at build time. Override with an
    explicit --runtime-file (or LDAI_RUNTIME_FILE via the linuxdeploy plugin).
  * entrypoint: auto-detect HOSTUID/HOSTGID from the workspace owner;
    explicit pass is now an optional override (previously required).
  * Dockerfile: pre-install gnupg, software-properties-common, imagemagick.

2026-05-22

  * Release v3.0.0

2024-11-26

  * Migrate appimagetool (replace AppImageKit with type2runtime)
    https://github.com/AppImage/type2-runtime

2024-11-23

  * Improve (and fix) entrypoint.sh;
    Breaking change: HOSTGID now needs to be set.

2024-02-12

  * Convert Action to a container
  * Release v2.0.0

2024-02-08

  * Release v1.1.1
  * Add arm/v7 support
  * Install more recent versions of CMake, Meson, and Ninja
