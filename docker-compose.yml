services:
  build:
    environment:
      HOSTUID: ${HOSTUID}
      HOSTGID: ${HOSTGID}
      VERSION: ${VERSION}
    image: ${REGISTRY_IMAGE}:${TAG}
    platform: ${PLATFORM}
    volumes:
      - $PWD:$WORKSPACE
    working_dir: $WORKSPACE
    command: $SCRIPT
